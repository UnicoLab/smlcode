api_version: blocks/v1
kind: pack
id: react
name: React Language Pack
description: >-
  React pack for Vite, Next or CRA — pipeline, quality (lint/tsc/test/build) and
  React-aware worker, tester and reviewer agents. Plain Node/TypeScript projects
  (no react dependency) get the `typescript` pack instead.
version: "1.1.0"
author: UnicoLab
license: MIT
language: typescript
tags: [react, typescript, vite, next, pack, marketplace]
icon: "⚛️"
shareable: true
spec:
  pipeline: react
  quality: react
  agents: [react-worker, react-tester, react-reviewer]
  skills: [atomic-coding, specialist-tester, specialist-worker, react-hooks, typescript-strict]
  pin_skills: true
  override_tester: react-tester
  override_worker: react-worker
